const lessonContainer = document.getElementById("lesson-container");
const lessonsGrid = document.getElementById("lessons-grid");
const lessonsContent = {
    lesson1: "<h2>Урок 1-2 – Введення</h2><p>Уяви, що ти граєшся з кубиками, і в тебе є три коробки для них: var, let, і const. Давай подивимося, як ці коробки працюють.</p><h3>1. var – стара коробка</h3><p>Уяви, що це чарівна коробка, куди ти можеш класти що завгодно, і навіть кілька разів змінювати, що всередині. Але ця коробка трохи непередбачувана: вона може випадково змінитися, навіть якщо ти цього не чекав.</p><p>❗️ Але іноді ця коробка створює проблеми: вона працює навіть поза межами кімнати, де її поклали. Це може заплутати тебе.</p><h3>2. let – новіша коробка</h3><p>Ця коробка більш слухняна: ти можеш змінювати її вміст, але тільки всередині своєї кімнати (в межах блоку коду, де її створили).</p><p>💡 Тепер у нас все під контролем! Якщо створити let у конкретному місці, він буде 'жити' тільки там, де його поклали, і не потрапить до інших кімнат.</p><h3>3. const – коробка, яка не змінюється</h3><p>Це коробка, куди можна покласти щось тільки один раз. Після цього вже не можна змінювати, що всередині. Уяви, що це твій улюблений ведмедик, який завжди залишається ведмедиком.</p><h4>Підсумок:</h4><ul><li>var – можна змінювати, але він може з'явитися там, де ти його не очікував.</li><li>let – можна змінювати, але тільки там, де його створили.</li><li>const – не можна змінювати взагалі, як улюблений ведмедик 🧸.</li></ul><h3>Типи даних</h3><h4>1. String – Рядок (текст)</h4><p>Уяви, що це ниточка з буквами або словами. Наприклад: 'слово', 'фраза'.</p><h4>2. Number – Число</h4><p>Це як твої улюблені цифри або кількість цукерок, які ти маєш. Тут можуть бути як цілі числа (5), так і дробові (2.5).</p><h4>3. Boolean – Так або Ні (Правда чи Брехня)</h4><p>Це як відповідь на просте запитання: «Ти любиш морозиво?» Відповіді можуть бути лише правда або неправда.</p><h4>4. null – Порожня коробка</h4><p>Уяви, що ти відкрив коробку, а вона порожня.</p><h4>5. undefined – Коробка ще не отримала вмісту</h4><p>Це як коли ти запитуєш: «Що буде в цій коробці?» А мама ще не вирішила.</p><h4>6. Array – Колекція речей (масив)</h4><p>Це як коробка, куди можна покласти багато іграшок одночасно.</p><h4>7. Object – Коробка з мітками</h4><p>Це як коробка з різними відділеннями.</p><h4>Підсумок:</h4><ul><li>String – текст («Привіт!»).</li><li>Number – числа (5, 2.5).</li><li>Boolean – правда чи брехня (true/false).</li><li>null – порожня коробка.</li><li>undefined – коробка без рішення.</li><li>Array – список речей.</li><li>Object – коробка з відділеннями.</li></ul><h3>Умови if...else</h3><p>Уяви, що ти граєш вдома, і тобі потрібно приймати рішення...</p><ul><li>if – якщо умова правильна (true), виконуємо цей код.</li><li>else – якщо умова неправильна (false), робимо щось інше.</li><li>else if – додаємо ще кілька варіантів.</li></ul><h3>alert та prompt</h3><h4>1. alert – Просто повідомлення</h4><p>Це як коли мама каже: «Час їсти!».</p><h4>2. prompt – Запитуємо щось у користувача</h4><p>Це як коли мама питає: «Що ти хочеш на вечерю?».</p><h4>Підсумок:</h4><ul><li>alert – Показує повідомлення, але не очікує відповіді.</li><li>prompt – Показує повідомлення та запитує відповідь.</li></ul>",
    lesson3: "<h2>Урок 3 – Функції: стрілочні, звичайні, замикання, інкапсуляція</h2><p>У цьому уроці детально розберемо типи функцій у JavaScript, їх відмінності, як передавати значення, коллбеки, замикання і як робити інкапсуляцію.</p><h3>1. Звичайні функції (function declaration та function expression)</h3><p>Опис: є два частих способи оголосити звичайну функцію: <strong>function declaration</strong> (function foo(a,b){...}) та <strong>function expression</strong> (const foo = function(a,b){...}). Function declaration піднімається (hoisted) — її можна викликати до оголошення в коді; function expression піднімається як змінна, тому виклик до оголошення призведе до помилки. Звичайні функції мають свій контекст <code>this</code> (визначається тим, як функцію викликали), мають доступ до об’єкта <code>arguments</code>, їх можна використовувати як конструктори з <code>new</code> або робити генераторами (function*).</p><h4>Приклад</h4><pre><code>function add(a,b){return a+b;} const sub = function(a,b){return a-b;}</code></pre><h3>2. Стрілочні функції</h3><p>Опис: синтаксично коротші: <code>const f = (a,b) =&gt; a+b;</code>. Якщо тіло — один вираз, return не потрібен; для блочного тіла використовуються фігурні дужки і явний return. Стрілочні функції не мають власного <code>this</code> — вони лексично захоплюють <code>this</code> з зовнішнього контексту; також вони не мають власного <code>arguments</code>, не можуть бути конструкторами (new з ними не працює) і не можуть бути генераторами.</p><h4>Приклад</h4><pre><code>const sum = (a,b) =&gt; a+b; const greet = name =&gt; 'Привіт, '+name;</code></pre><h3>3. Головні відмінності</h3><ul><li><strong>this</strong>: звичайні функції мають динамічний this; стрілочні — лексичний (використовують this зовнішнього оточення).</li><li><strong>arguments</strong>: доступний у звичайних; у стрілочних його немає (можна використовувати rest-параметри ...args).</li><li><strong>hoisting</strong>: декларації піднімаються; функціональні вирази як змінні — ні.</li><li><strong>конструктор</strong>: звичайні функції можуть бути викликані з new; стрілочні — ні.</li></ul><h3>4. Коллбеки</h3><p>Коллбек — функція, яку передають як аргумент іншій функції для виклику пізніше. Використовується для подій, обробки результатів або асинхронної роботи. Коллбеки бувають синхронні і асинхронні. Недолік: велика вкладеність (callback-hell), через що з'явилися Promises і async/await.</p><h4>Приклад синхронного коллбека</h4><pre><code>function repeat(n,fn){for(let i=0;i&lt;n;i++){fn(i);}} repeat(3,i =&gt; console.log(i));</code></pre><h4>Приклад асинхронного коллбека</h4><pre><code>setTimeout(() =&gt; console.log('через 1с'),1000);</code></pre><p>У Node-патерні часто використовують error-first callbacks: <code>fn(err, result)</code>, але в сучасному коді частіше застосовують Promises/async-await для читабельності.</p><h3>5. Передавання значень у функції</h3><p>У JavaScript аргументи завжди передаються за значенням. Для примітивів (number, string, boolean, null, undefined, symbol) це копія значення — зміни всередині функції не впливають на зовнішню змінну. Для об'єктів і масивів передається копія посилання на об'єкт: якщо функція мутує внутрішній стан об'єкта (наприклад, змінює властивості), ці зміни будуть видимі зовні; якщо ж параметр переприсвоїти (param = {}), зовнішня змінна залишиться без змін.</p><h4>Приклад</h4><pre><code>function changeNum(x){x=5;} let a=1; changeNum(a); // a все ще 1 function mutate(obj){obj.name='New';} const o={name:'Old'}; mutate(o); // o.name тепер 'New'</code></pre><h3>6. Замикання (closures)</h3><p>Замикання — коли внутрішня функція пам'ятає лексичне оточення зовнішньої, навіть після того як зовнішня функція завершила виконання. Це дозволяє створювати приватні змінні і фабрики функцій.</p><h4>Приклад лічильника</h4><pre><code>function counter(){let count=0; return function(){count++; return count;}} const c = counter(); c(); // 1 c(); // 2</code></pre><p>Замикання часто використовують для інкапсуляції стану: зовнішній код не може напряму змінити приватну змінну, тільки через надані методи.</p><h3>7. Інкапсуляція</h3><p>Інкапсуляція в JS реалізується через замикання (функціональні модулі, IIFE), або через класи з приватними полями (<code>#privateField</code>) та символи. Приклад: модуль, який повертає об'єкт з методами, але зберігає приватні змінні всередині замикання. Переваги: контроль доступу і захист від випадкових змін.</p><h3>8. Практичні поради</h3><ul><li>Використовуйте стрілочні функції для коротких колбеків та коли потрібне лексичне this.</li><li>Використовуйте звичайні функції, якщо вам потрібен власний this, arguments або можливість бути конструктором.</li><li>Для приватних даних застосовуйте замикання або приватні поля класів.</li><li>Уникайте мутацій вхідних об'єктів — робіть копії, якщо потрібно.</li><li>Для асинхронності віддавайте перевагу Promises/async-await замість глибоких вкладених коллбеків.</li></ul><h3>9. Короткий приклад різниці this</h3><pre><code>const obj = {val:10, method:function(){return this.val;}, arrow:() =&gt; this.val}; obj.method(); // 10 obj.arrow(); // undefined або глобальний this</code></pre><h3>Висновок</h3><p>Функції в JS дуже гнучкі: стрілочні дають компактність і стабільний лексичний this, звичайні функції надають більше можливостей (hoisting, arguments, конструктор). Замикання і інкапсуляція дозволяють будувати безпечні модулі з приватним станом. Коллбеки корисні, але для читабельності асинхронного коду краще використовувати Promises та async/await.</p>",
    lesson4: "<h2>Урок 4 – DOM та події</h2><p>У цьому уроці розглянемо, що таке DOM (Document Object Model), як з ним працювати, а також різні способи обробки подій у JavaScript – старі (onClick та інші атрибути) і сучасні (addEventListener).</p><h3>1. DOM (Document Object Model)</h3><p>DOM – це структура документа HTML, представлена у вигляді дерева об’єктів. Кожен елемент (тег, текст, атрибут) стає вузлом цього дерева. За допомогою JavaScript ми можемо знаходити, змінювати, додавати або видаляти елементи.</p><h4>Основні методи доступу</h4><ul><li><code>document.getElementById('id')</code> – пошук за id.</li><li><code>document.querySelector('selector')</code> – повертає перший елемент за CSS-селектором.</li><li><code>document.querySelectorAll('selector')</code> – повертає список усіх елементів.</li><li><code>element.textContent / innerHTML</code> – змінюють текст або HTML-вміст.</li><li><code>element.style.property</code> – зміна стилів напряму.</li></ul><h3>2. Події</h3><p>Подія – це сигнал про те, що щось сталося: клік, наведення, натискання клавіші, завантаження сторінки тощо. JS дозволяє «слухати» події та реагувати на них.</p><h3>3. Старий підхід – обробники у вигляді атрибутів</h3><p>Можна прямо в HTML-атрибуті задати функцію для події. Наприклад:</p><pre><code>&lt;button onclick='alert(\"Привіт!\")'&gt;Натисни&lt;/button&gt;</code></pre><p>Або через властивість елемента:</p><pre><code>const btn = document.getElementById('myBtn'); btn.onclick = function(){ alert('Клік!'); };</code></pre><p>Недоліки: можна призначити лише один обробник; логіка змішується з HTML; важче масштабувати.</p><h3>4. Сучасний підхід – addEventListener</h3><p>Метод <code>addEventListener</code> дозволяє підписатися на подію, додати кілька обробників, а також налаштовувати фази виконання (capturing/bubbling).</p><pre><code>const btn = document.getElementById('myBtn'); btn.addEventListener('click', () =&gt; { console.log('Клік!'); });</code></pre><p>Можна зняти обробник за допомогою <code>removeEventListener</code>.</p><h3>5. Види подій</h3><ul><li><strong>Мишка:</strong> click, dblclick, mouseover, mouseout, contextmenu.</li><li><strong>Клавіатура:</strong> keydown, keyup, keypress.</li><li><strong>Форма:</strong> submit, input, change, focus, blur.</li><li><strong>Вікно/документ:</strong> load, resize, scroll.</li></ul><h3>6. Поширення подій (event bubbling & capturing)</h3><p>Події в DOM рухаються у два етапи: спочатку вниз (capturing), потім вгору (bubbling). За замовчуванням обробники реагують на фазі bubbling, але можна ввімкнути capturing третім аргументом в <code>addEventListener</code>.</p><pre><code>document.getElementById('child').addEventListener('click', e =&gt; { alert('Child'); }, true);</code></pre><h3>7. Практичні поради</h3><ul><li>Не використовуйте inline-обробники (onclick) у сучасних проектах.</li><li>Використовуйте <code>addEventListener</code>, щоб код був чистішим і підтримував кілька обробників.</li><li>Для складних інтерфейсів застосовуйте делегування подій (встановлюйте обробник на батьківський елемент і перевіряйте <code>event.target</code>).</li></ul><h3>Висновок</h3><p>DOM дозволяє динамічно змінювати сторінку, а події – реагувати на дії користувача. Старі методи (onclick) прості, але обмежені. Сучасний підхід (<code>addEventListener</code>) більш гнучкий, безпечний і рекомендований для використання у всіх сучасних проєктах.</p>",
    lesson5: "<h2>Урок 5 – Цикли, масиви та спред-оператор</h2><p>У цьому уроці розглянемо цикли, базові операції з масивами, поняття мутації коду та використання спред-оператора.</p><h3>1. Цикли</h3><h4>for</h4><p>Виконує код певну кількість разів, поки умова істинна.</p><pre><code>for (let i = 0; i < 5; i++) { console.log(i); }</code></pre><h4>while</h4><p>Виконує код доти, доки умова істинна.</p><pre><code>let i = 0; while (i < 5) { console.log(i); i++; }</code></pre><h4>do...while</h4><p>Виконує код мінімум один раз, а потім перевіряє умову.</p><pre><code>let i = 0; do { console.log(i); i++; } while (i < 5);</code></pre><h4>for...of</h4><p>Зручний для перебору елементів масиву.</p><pre><code>const arr = [1,2,3]; for (const num of arr) { console.log(num); }</code></pre><h4>for...in</h4><p>Використовується для перебору ключів об'єкта.</p><pre><code>const obj = {a:1,b:2}; for (const key in obj) { console.log(key, obj[key]); }</code></pre><h3>2. Масиви (базове)</h3><p>Масив – структура для зберігання колекції даних.</p><pre><code>const fruits = ['apple','banana','orange']; console.log(fruits[0]);</code></pre><h3>3. Спред-оператор (...)</h3><p>Дозволяє розгортати масив чи об'єкт.</p><pre><code>const arr1 = [1,2]; const arr2 = [...arr1,3,4]; console.log(arr2); // [1,2,3,4]</code></pre><h3>4. Мутація</h3><p>Мутація означає зміну існуючого об'єкта чи масиву напряму.</p><pre><code>const arr = [1,2]; arr.push(3); console.log(arr); // [1,2,3] – масив мутував</code></pre><p>Щоб уникати мутацій, використовують копії:</p><pre><code>const arr = [1,2]; const newArr = [...arr,3]; console.log(newArr); // [1,2,3]</code></pre><h3>Підсумок</h3><ul><li>Цикли дозволяють багаторазово виконувати код.</li><li>Масиви зберігають послідовність даних.</li><li>Спред-оператор розгортає структури.</li><li>Мутація змінює існуючі дані напряму, що не завжди добре.</li></ul>",
    "lesson6": "<h2>Урок 6 – Об'єкти та методи масивів</h2><p>Масиви в JavaScript мають безліч вбудованих методів. Тут розберемо приклади та коментарі до кожного методу.</p><h3>0. Масиви та об'єкти</h3><p>Приклади оголошення масивів та об'єктів:</p><pre><code>// const arr=[5,5,6,7,768,56]\n// {key:'value'}\n// { name:'artem', age:25 }\nconst person={name:'artem',age:25};\nconsole.log(person.name); // artem\nconsole.log(person.age); // 25\nconst people=[{name:'artem',age:25,zp:1000},{name:'olya',age:23,zp:1200},{name:'masha',age:18,zp:800}];</code></pre><h3>1. sort</h3><p>sort сортує масив на місці. Він приймає два аргументи (a,b) для порівняння. Приклади: (a,b)=>a-b для зростання, (a,b)=>b-a для спаду. Створює новий відсортований масив.</p><pre><code>let number=[5,6,7,8,1,2,3,4];\nlet sortedNumbers=number.sort((a,b)=>a-b);\nlet sortedPeopleByZp=people.sort((a,b)=>a.zp-b.zp);</code></pre><h3>2. forEach</h3><p>forEach перебирає масив, застосовуючи функцію до кожного елемента. Не створює новий масив, не можна зупинити, не повертає результат, не працює з async. Приймає 3 аргументи: елемент, індекс, масив.</p><pre><code>const prev=document.querySelector('.prev');\npeople.forEach(person=>{ const li=document.createElement('li'); li.textContent=`${person.name} (${person.age}) - ${person.zp}`; prev.appendChild(li); });\n// Пояснення:\n// 1. Ми перебираємо масив people за допомогою forEach\n// 2. Для кожного елемента масиву створюємо li\n// 3. Додаємо текст з інформацією про людину\n// 4. Додаємо li до ul з класом prev</code></pre><h3>3. find</h3><p>find шукає перший елемент, який відповідає умові, або повертає undefined.</p><pre><code>let findOlya=people.find(person=>person.name==='olya');\n// {name:'olya', age:23, zp:1200}</code></pre><h3>4. filter</h3><p>filter повертає новий масив з елементами, що відповідають умові. Приклади застосування: фільтрація, створення нового масиву, зміна елементів, видалення, пошук на сайті, робота з формами, API, базами даних, інтернет-магазинами, соцмережами, месенджерами.</p><pre><code>let filteredPeopleByAge=people.filter(person=>person.age<=20);\nlet filteredPeopleByZp=people.filter(person=>person.zp>=1000);\nlet deletePerson=people.filter(person=>person.name!=='olya');\n// Пояснення:\n// 1. Ми перебираємо масив people за допомогою filter\n// 2. Перевіряємо умову\n// 3. Додаємо людину до нового масиву, якщо умова виконується\n// 4. Пропускаємо, якщо умова не виконується\n// 5. В результаті отримуємо новий масив без olya</code></pre><h3>5. map</h3><p>map створює новий масив на основі певних умов, повертає масив після виконання функції для кожного елемента.</p><pre><code>let peopleNames=people.map(person=>person.name);\nlet num=[1,2,3,4,5];\nlet xTwo=num.map(num=>num*2);\n// Пояснення:\n// 1. Перебираємо масив people\n// 2. Створюємо новий масив з іменами\n// 3. Для чисел створюємо новий масив, де кожен елемент помножений на 2</code></pre><h3>6. reduce</h3><p>reduce акумулює значення масиву в одне (сума, добуток, перетворення).</p><pre><code>const totalZP=people.reduce((sum,person)=>sum+person.zp,0);\nlet sum=0; for(let i=0;i<people.length;i++){sum+=people[i].zp;}\n// Пояснення:\n// 1. Перебираємо масив people за допомогою reduce\n// 2. Додаємо зарплату кожної людини до sum\n// 3. Початкове значення 0\n// 4. В результаті totalZP = 3000, sum = 3000</code></pre><h3>7. some</h3><p>some перевіряє, чи хоча б один елемент відповідає умові (true/false).</p><pre><code>let hasHighSalary=people.some(person=>person.zp>1000);\n// true</code></pre><h3>8. reverse</h3><p>reverse перевертає масив на місці.</p><pre><code>[1,2,3].reverse();</code></pre><h3>9. splice</h3><p>splice мутує масив, видаляє, замінює або додає елементи.</p><pre><code>const arr=[1,2,3]; arr.splice(1,1,'X');</code></pre><h3>10. slice</h3><p>slice повертає копію частини масиву, не мутує його.</p><pre><code>[1,2,3,4].slice(1,3);</code></pre><h3>11. concat</h3><p>concat створює новий масив, об'єднуючи кілька.</p><pre><code>[1,2].concat([3,4]);</code></pre><h3>12. flat</h3><p>flat «розплющує» вкладені масиви.</p><pre><code>[1,[2,[3]]].flat(2);</code></pre><h3>13. flatMap</h3><p>flatMap поєднує map + flat(1).</p><pre><code>[1,2].flatMap(n=>[n,n*2]);</code></pre><h3>Підсумок</h3><ul><li>forEach – перебір, не повертає</li><li>map/filter – створюють нові масиви</li><li>find – пошук першого елемента</li><li>reduce – для обчислень і перетворень</li><li>some – перевірка на хоча б один елемент</li><li>sort/reverse/splice – мутують масив</li><li>slice/concat – копії без мутації</li><li>flat/flatMap – для вкладених структур</li></ul>"


};
const lessonsContainer = document.getElementById("lessons-grid");
const viewer = document.getElementById("lesson-viewer");
const viewerTitle = document.getElementById("viewer-title");
const viewerText = document.getElementById("viewer-text");
const backBtn = document.getElementById("back-btn");

Object.keys(lessonsContent).forEach(lessonId => {
    const fileDiv = document.createElement("div");
    fileDiv.className = "file";
    fileDiv.dataset.id = lessonId;

    const tempDiv = document.createElement("div");
    tempDiv.innerHTML = lessonsContent[lessonId];
    const title = tempDiv.querySelector("h2")?.textContent || lessonId;

    fileDiv.textContent = `📄 ${title}`;
    lessonsContainer.appendChild(fileDiv);

    fileDiv.addEventListener("click", () => {
        viewerTitle.textContent = title;
        viewerText.innerHTML = lessonsContent[lessonId];
        lessonsContainer.classList.add("hidden");
        viewer.classList.remove("hidden");
    });
});

backBtn.addEventListener("click", () => {
    viewer.classList.add("hidden");
    lessonsContainer.classList.remove("hidden");
});
