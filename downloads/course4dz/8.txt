ДЗ:

Виконати GET-запит до будь-якого публічного API через Axios та показати дані у компоненті.

Написати POST-запит для відправки даних на сервер (можна JSONPlaceholder).

Використати interceptors для логування запитів або обробки помилок.